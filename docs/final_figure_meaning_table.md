# Final figure meaning table (candidate paths for manuscript)

| final_figure_id | file_to_use | section | what_it_shows | main_conclusion | source_data | should_insert_into_word | caption_suggestion_cn |
|-----------------|------------|---------|---------------|-----------------|-------------|-------------------------|----------------------|
| `fig3_system_setup` | **`figures/patent/optical_path_clean.png`** (primary candidate; **`optical_path_render.png`** / **`fig1_system.png`** as alternates — optical review required) | 3.1 | Dual-wavelength excitation schematic: 532 nm side-polished launch, 650 nm end-face launch, speckle imaging to CCD. | Reader understands geometry distinguishing common-mode from channel-specific modes. | Patent / layout sources — **no** tabular experiment file in repo | **After** PI confirms arrows, labels, and wavelength placements | **图3** 侧抛塑料光纤 POF-PUF 实验装置与双通道激励原理示意图（532 nm 侧向写入，650 nm 端面激励，输出散斑由透镜成像至 CCD）。 |
| `fig4_length_optimization` | **`figures_competition/fig4_length_optimization.png`** (Word); **`…/fig4_length_optimization.pdf`** or **`.svg`** (layout) | 3.2(1) | Transmission loss (red/green), intra- vs inter-class distance with **inter/intra** ratio on right axis, and pixel entropy vs **total fiber length**. | Total length **9 cm** balances green loss (~**28.39 dB**), maximal **inter/intra ratio ~1.5653**, and entropy ~**6.183 bit**. | `results/length_optimization_green/tables/per_length_summary.csv`; `results/length_optimization_green/optimal_length.json`; `scripts/generate_competition_figures.py` | **Yes** (placeholder 图4) | **图4** 不同总长度侧抛光纤上红光/绿光损耗、类内与类间 L₂ 距离及 **类间/类内距离比**（右轴）、输出散斑像素熵的对比；竖线标示最优总长度 9 cm（非“绿程 9 cm”）。 |
| `fig7_dual_channel_characterization` | **`figures_competition/fig7_dual_channel_characterization.png`** (+ pdf/svg) | 3.2(3) | (a) Red vs green intra-class correlation; (b) normalized correlation retention after micro-bending; (c) radial intensity profiles from representative frames. | Green channel is more perturbation-sensitive than red; radial extent differs — supports dual-channel excitation narrative. | **Mixed:** `long_term_stability/` JPEGs + `videocapture/.../A.avi` + **manuscript summary** constants in `generate_competition_figures.py` (`manifest.csv` notes) | **Yes**, with caption caveats where panels use summary values | **图7** 双通道激励特性验证：（a）时域稳定性（类内相关系数）；（b）微弯扰动前后相对相关性；（c）侧向绿光激励与端面红光的归一化径向平均强度对比（视频帧代表）。 |
| `fig8_common_mode_suppression` | **`figures_competition/fig8_common_mode_suppression.png`** (+ pdf/svg) | 3.2（共模抑制段后） | (a) CV of raw green intensity vs **η = G/R**; (b) illustrative reinstall robustness as intra-class NCC gain for η vs raw green feature. | Common-mode power drift is strongly suppressed (~**8.9×** CV reduction narrative); η improves reinstall NCC by ~**+28%** vs scaled raw baseline in proposal constants. | **Summary constants** in `generate_competition_figures.py`; raw `power_common_mode/` **exists** but **not** used in current script | **Yes** if editor accepts summary-statistics caption; else regenerate from `power_common_mode/` | **图8** 共模扰动抑制：（a）原始绿光强度与 **η = G/R** 的波动系数对比；（b）机械重装前后类内相关系（示意）显示 η 特征相对原始绿光特征的增益。 |
| `fig12_recognition_performance` | **`figures/fig_auth_matrix.png`** + **`figures_publication/publication_fig04_cross_fiber_auth.png`** (choose one primary layout) | 3.3 | Authentication matrix / cross-fiber rejection vs same-fiber accuracy. | Classifier accepts enrolled fiber, rejects others at high confidence. | `results/fiber_auth/auth_matrix.json`; `scripts/make_paper_figures.py` / `scripts/make_publication_figures.py` | **Yes** — **not** in §3.2 | **图12（示例编号）** 同纤识别与跨纤拒绝：认证矩阵 / 准确率分布（与 §3.3 算法性能对应）。 |

---

### What this table excludes (by design)

- **`publication_fig03/04_length_optimization`:** wrong panel topology for current 策划书 Fig. 4; **archive / supplementary** only.
- **`figures/fig_green_length_*`:** legacy `length_optimize_current` pipeline — **do not use** for current Fig. 4 metrics.
