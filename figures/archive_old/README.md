# Archived figure assets

## `legacy_green_partial_32/`

Contains `fig_green_length_*.png` generated via `run_partial_length_analysis` / `green_partial_32` pipeline (see `figures/regeneration_manifest.json`). **Do not use** for the final `length_optimization_green` manuscript figure.

## Root-level Chinese report

`图表与实验结果分析报告.md` — archived because reports belong outside the English figure tree; content may reference older partial-length runs.

## Policy

Files here are **retained for provenance**. New paper figures must cite CSV/JSON inputs under `results/length_optimization_green/` or other verified directories.
